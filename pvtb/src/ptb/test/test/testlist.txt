# Cache Regression Test List
# Format: test_group = testname_pattern

# Basic cache tests
cache_sanity = cache_sanity_test
cache_basic = cache_basic_test

# Performance tests
cache_perf = cache_perf_test(128|256|512)
cache_load = buffer_load_ipw(128|64)_wpg(4)_dwpi(1|2|4)_comp(1)_hit(TCC)

# Stress tests
cache_stress = cache_stress_test(1000|2000|5000)
